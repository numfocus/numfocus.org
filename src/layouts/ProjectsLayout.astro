---
import BaseLayout from './BaseLayout.astro';

interface Props {
  title: string;
  seo: any;
}

const { title, seo } = Astro.props;
---

<BaseLayout title={title} seo={seo}>
  <slot />
</BaseLayout>
