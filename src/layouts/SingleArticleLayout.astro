---
import type { Seo } from 'env';
import BaseLayout from './BaseLayout.astro';
import Layout from './Layout.astro';

interface Props {
  title: string;
  seo: Seo;
}

const { title, seo } = Astro.props;
---

<BaseLayout title={title} seo={seo}>
  <slot />
</BaseLayout>
